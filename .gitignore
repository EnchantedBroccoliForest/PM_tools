node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json

# Per-run artifacts from the Phase 6 regression eval harness. The
# committed baseline lives at eval/baseline.json and is NOT ignored;
# only the ephemeral per-run dumps under eval/out/ are excluded.
eval/out/
