{
  "$schema": "./pipeline-config.schema.json",
  "description": "Fast, low-cost configuration. Skips evidence gathering and uses partial verification. Suitable for rapid iteration on draft wording before committing to a full review.",

  "models": {
    "drafter": "openai/gpt-4o-mini",
    "reviewers": [
      { "id": "openai/gpt-4o-mini", "name": "GPT-4o Mini" }
    ],
    "judge": "openai/gpt-4o-mini"
  },

  "options": {
    "aggregation": "majority",
    "escalation": "selective",
    "evidence": "none",
    "verifiers": "partial",
    "deliberation": "off"
  }
}
